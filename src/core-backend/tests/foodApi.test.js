const foodApi = require('../external-services/foodApi');

describe('Search Food', () => {
    it('should return an arry of food passing a portuguese query', () => {

    })

    it('should return an empty array if no food is found', () => {

    })

    it('should return null if an error is thrown', () => {
        
    })
});